#!/usr/bin/python3
def new_in_list(my_list, idx, element):
    new_list = my_list[:]
    if idx < 0:
        return new_list[idx] = element
    elif idx > (len(my_list) - 1):
        return new_list[idx] = element
    else:
        return new_list
